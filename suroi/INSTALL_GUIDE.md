# 🎮 최후의생존 (Last Survival) - 설치 가이드

## 📋 개요
surviv.io에서 영감을 받은 오픈소스 2D 배틀로얄 게임입니다.

---

## ⚙️ 시스템 요구사항

- **Node.js**: v18 이상
- **npm**: v9 이상
- **Git**: 최신 버전
- **OS**: Windows 10/11, macOS, Linux

---

## 📥 설치 방법

### 1단계: 저장소 복제
```bash
git clone https://github.com/YOUR_USERNAME/last-survival.git
cd last-survival/suroi
```

### 2단계: 의존성 설치
```bash
npm install
```

### 3단계: 클라이언트 빌드
```bash
cd client
npm install
cd ..
```

### 4단계: 서버 빌드
```bash
cd server
npm install
cd ..
```

---

## 🚀 실행 방법

### 개발 모드 (권장)
```bash
# 터미널 1: 서버 실행
npm run server:solo

# 터미널 2: 클라이언트 실행
npm run dev
```

### 접속
- **게임 클라이언트**: http://localhost:5000
- **관리자 패널**: client/admin/index.html 열기

---

## ⚠️ 중요: 포트 번호 설정

### 🚫 절대 변경하지 마세요!

현재 설정된 포트 번호는 모든 파일에서 동기화되어 있습니다.
변경 시 게임이 작동하지 않습니다.

| 용도 | 포트 번호 |
|------|-----------|
| **클라이언트 Dev 서버** | **5000** |
| **솔로 메인 서버** | **4000** |
| **솔로 워커 서버** | **4001** |
| **듀오 메인 서버** | **4010** |
| **듀오 워커 서버** | **4011** |
| **스쿼드 메인 서버** | **4020** |
| **스쿼드 워커 서버** | **4021** |

### 포트 관련 파일 (변경 금지!)
- `server/config.json`
- `server/config-solo.json`
- `server/config-duo.json`
- `server/config-squad.json`
- `client/src/scripts/config.ts`
- `client/vite/vite.common.ts`
- `client/admin/admin.js`
- `client/admin/index.html`

---

## 🎮 게임 모드별 실행

### 솔로 모드
```bash
npm run server:solo
```

### 듀오 모드
```bash
npm run server:duo
```

### 스쿼드 모드
```bash
npm run server:squad
```

---

## 🤖 AI 봇 설정

관리자 패널(`client/admin/index.html`)에서:
1. 브라우저로 파일 열기
2. "자동 봇 수" 설정
3. "설정 저장" 클릭

---

## 📁 폴더 구조

```
suroi/
├── client/          # 게임 클라이언트
│   ├── admin/       # 관리자 패널
│   ├── src/         # 소스 코드
│   └── rules/       # 규칙 및 튜토리얼
├── server/          # 게임 서버
│   ├── src/         # 서버 소스
│   └── config.json  # 서버 설정
└── common/          # 공통 코드
```

---

## ❓ 문제 해결

### 포트 충돌
다른 프로그램이 포트를 사용 중이면:
```bash
# Windows
netstat -ano | findstr :5000
taskkill /PID [PID번호] /F
```

### node_modules 오류
```bash
rm -rf node_modules
npm install
```

---

## 📧 문의
- 이메일: kwonsh04200@naver.com

---

**버전**: 1.0.0  
**최종 업데이트**: 2024-12-25
